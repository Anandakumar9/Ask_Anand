name: mobile
description: StudyPulse AI-Powered Exam Preparation Platform
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dio: ^5.3.3
  flutter_secure_storage: ^9.0.0
  provider: ^6.0.5
  google_fonts: ^6.1.0
  percent_indicator: ^4.2.3
  lucide_icons: ^0.257.0
  confetti: ^0.7.0
  shared_preferences: ^2.2.2
  intl: ^0.18.1
  flutter_spinkit: ^5.2.0
  url_launcher: ^6.2.1  # For rate us dialog (Play Store / App Store links)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

