# ── StudyPulse Backend - Production Dependencies ──
# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database (SQLAlchemy async + SQLite for dev)
sqlalchemy[asyncio]>=2.0.25
aiosqlite>=0.19.0

# Auth & Validation
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1
pydantic[email]>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.1.0

# Async HTTP Client (for Ollama LLM)
httpx>=0.26.0

# Vector Database + Embeddings (Qdrant + FastEmbed ONNX)
qdrant-client>=1.12.0
fastembed>=0.4.0

# Cache (Redis async)
redis[hiredis]>=5.0.1

# Logging
python-json-logger>=2.0.7

# Environment
python-dotenv>=1.0.0
